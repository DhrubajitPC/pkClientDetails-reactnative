# pkClientDetails-reactnative

This is a small react native app that is a form with fields and a photoUploader.
It uses node, mongo and express in the backend to expose api to the front-end react-native app.
